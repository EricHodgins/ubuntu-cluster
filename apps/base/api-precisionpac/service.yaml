apiVersion: v1
kind: Service

metadata:
  name: api-precisionpac  

spec:
  selector:
    app: api-precisionpac

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP